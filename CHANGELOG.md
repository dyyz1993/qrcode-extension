# Changelog




## [1.2.1] - 2026-07-19

### ✨ 智能识别：地址栏是图片时自动切到图片 Tab

popup 打开时检测当前标签页 URL，识别是不是图片：

- **URL 后缀明显**（.png/.jpg/.jpeg/.webp/.gif/.bmp/.svg/.avif）→ 立即识别
- **URL 看起来像图片**（query 含 image/photo/pic/img 等）→ 发 HEAD 请求验证 Content-Type
- **识别为图片**：自动切到「📷 图片」Tab，显示预览 + 提示「点按钮上传」
- **识别为普通网页**：保持默认「🔗 链接」Tab（原逻辑）

技术上利用扩展 SW fetch 绕过 CORS 和多数防盗链，对图床直链特别有效。
不识别时也不影响其他 Tab 的正常使用。

---

## [1.2.0] - 2026-07-19

### ✨ 新增：图片上传 → 扫码看图

支持三种入口上传图片，生成短链二维码，手机扫码打开图片页（长按可保存）。

- **popup 新增「📷 图片」Tab**
  - 「📁 选择图片」按钮 + 拖拽上传
  - 在 Tab 内 Ctrl+V 粘贴截图（支持系统截屏）
  - 选完显示预览 + 大小提示，5MB 上限
- **「📝 文本」Tab 粘贴图片自动切换**
  - 在文本框粘贴图片时，自动切到图片 Tab 走上传流程
- **右键网页图片 → 「📷 此图传到服务器生成二维码」**
  - 利用 SW fetch 绕过 CORS 和多数防盗链（请求带着页面 Cookie/Referer）
  - 对服务器来说请求就像来自当前浏览器，大多数图床都能抓到

### 🖥️ 服务端：图片存储端点

- 新增 `POST /api/upload` — multipart 图片上传，返回短码（每 IP 每分钟 5 次频率限制）
- 新增 `GET /img/:code` — 返回原始图片二进制，带正确 Content-Type + 缓存头
- 新增 `GET /s/:code`（按类型分派）—— 图片类型返回带 `<img>` 的展示页 + 右上角「💾 保存」按钮
- 支持 PNG/JPEG/GIF/WebP/BMP（SVG 暂不支持，安全考虑）
- 服务端用 `http.DetectContentType` 嗅探真实 MIME（防伪造扩展名）
- 存储分层：元数据在 `links.json`，图片二进制在 `data/images/<code>.<ext>`
- 7 天过期，过期/惰性清理同步删文件
- 大小硬限 5MB（前端 + 后端双校验）

### 🔧 改进

- `config.js` 同时挂载到 `window` 和 `self`，支持 Service Worker 加载
- popup.js 在 shorten/upload 前重新读一次配置（用户在设置页改的值立即生效）
- background.js 新增 `loadConfig()`，SW 也能读 `chrome.storage.sync` 里的配置

---

## [1.1.1] - 2026-07-18

### ✨ 新增：设置页（Options Page）

- popup 右上角加 **⚙️ 齿轮按钮**，点击打开设置页
- 可配置「服务器地址」和「访问令牌」
- 配置存 `chrome.storage.sync`，**通过 Chrome 账号自动跨设备同步**
- **🧪 测试连接**按钮：一键验证服务端是否可达
- **↩️ 恢复默认**按钮：一键回到内置默认服务器
- 保留 `config.js` 作为开箱即用的默认值——不配置也能直接用

---

## [1.1.0] - 2026-07-18

### ✨ 新增：文本转短链二维码（搭配远程服务端）

- **弹窗新增 Tab 切换**：「🔗 链接」（原逻辑）和「📝 文本」（新功能）
- **文本 Tab**：粘贴任意长文本 → 远程服务转短链 → 生成短链二维码
  - 手机扫码后打开一个 HTML 页面，展示文本内容
  - 右上角「📋 复制」按钮，一键复制全文（手机端拇指易达）
  - 内容保留 7 天后自动清理
- **触发方式**：回车直接生成 / Shift+Enter 换行 / 点「🔲 生成二维码」按钮
- **失败降级**：服务端不可达时自动降级为本地编码原始文本（依然可用，只是二维码较密）

### 🖥️ 新增：Go 远程短链服务（`server/`）

- 单文件 Go HTTP 服务，零第三方依赖（纯标准库）
- 端点：
  - `POST /api/shorten` — 接收文本，返回 6 位 base62 短码（需 `X-Auth-Token` 鉴权）
  - `GET /s/:code` — 返回内联 HTML 展示页（含一键复制按钮）
  - `GET /api/health` — 健康检查
- JSON 文件存储，原子写入（tmp + rename）
- 每 IP 每分钟 10 次频率限制
- 过期清理：启动扫描 + 每小时循环 + 惰性删除
- 5.1MB 静态二进制，内存占用约 4MB

### 🏗️ 项目结构重构

- `extension/` — Chrome 扩展代码整体迁入（原有功能完全保留）
- `server/` — Go 远程短链服务
- `extension/config.js` — 集中管理远程服务 BASE_URL / AUTH_TOKEN
- 打包 workflow 改为只打包 `extension/`（服务端不进扩展包）

### 🔧 其他改进

- 服务端断开时扩展优雅降级，不报错卡死
- 一键部署脚本 `server/deploy.sh`（交叉编译 + 上传 + pm2 重启）

---

## [1.0.0] - 2025-06-27

### 🎉 首个正式版本

#### ✨ 新增功能

- **弹窗生成二维码**：点击工具栏图标，显示当前页面 URL 及对应二维码，支持输入自定义内容后回车刷新
- **右键解析图片二维码**：在 `<img>` 图片上右键，选择"解析图片中的二维码"，自动识别并显示内容
- **右键解析 Canvas 二维码**：在 Canvas 元素上右键，选择"解析 Canvas 中的二维码"，捕获画布内容进行解码
- **右键快捷生成**：
  - 链接上右键 → 生成链接的二维码
  - 选中文字右键 → 生成文字的二维码
  - 页面空白处右键 → 生成当前页面 URL 的二维码
- **结果浮层**：解析/生成结果以浮层展示，支持复制内容、打开链接、下载二维码图片
- **UTF-8 完整支持**：中文内容生成与解码无乱码

#### 🛠️ 技术实现

- Manifest V3 规范
- Service Worker 后台处理
- Content Script 捕获页面元素数据
- jsQR 纯 JS 解码
- OffscreenCanvas 无头渲染

#### 📦 打包发布

- GitHub Actions 自动打包为 `.zip` 发布包
- 提供 Release 下载
