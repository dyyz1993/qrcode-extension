# Changelog




## [1.3.2] - 2026-07-20

### 🐛 修复：WebRTC 检测优先选真实 WiFi IP，避开虚拟网卡

之前 WebRTC 检测到多个 IP 时，可能优先返回虚拟网卡 IP（如 Docker/OrbStack 的 `192.168.64.1`），导致手机扫码连不上。

**新增虚拟网卡网段黑名单**：
- `192.168.64.x`（OrbStack / lima / colima）
- `192.168.65.x`（Docker Desktop 新版）
- `192.168.99.x`（Docker Desktop 旧版）
- `192.168.122.x` / `192.168.123.x`（libvirt）
- `192.168.39.x`（minikube）

**新的选择优先级**：
1. `192.168.x.x` 且不在虚拟黑名单（最可能是 WiFi 局域网）
2. `10.x.x.x`（企业局域网）
3. `172.16-31.x.x`
4. 其他（含虚拟网卡，总比没有强）

---

## [1.3.1] - 2026-07-20

### ✨ 新增：localhost URL 自动转局域网 IP

打开 `localhost` / `127.0.0.1` 页面时，扩展会自动把地址换成局域网 IP，手机扫码即可访问本地 dev server。

- **自动检测**：用 WebRTC ICE candidate 收集本机局域网 IP（纯前端，无后端请求）
- **优先级**：手动配置（设置页）> WebRTC 检测
- **支持私有 IP 段**：10.x / 172.16-31.x / 192.168.x
- **URL 完整保留**：端口、路径、query 参数都保留
- **设置页新增「局域网 IP」输入项**：WebRTC 失败时的 fallback（如 Chrome 开了 mDNS 混淆）
- 跨设备同步（chrome.storage.sync）

---

## [1.3.0] - 2026-07-20

### 新增：通用文件上传（APK/zip/pdf/exe 等任意文件）

popup 新增第四个 Tab「📎 文件」，支持任意文件类型上传，生成扫码下载二维码。

- **popup 新增「📎 文件」Tab**：
  - 选择本地文件 / 拖拽上传
  - 文件信息卡：图标（按扩展名 emoji）+ 文件名 + 大小
  - 单文件上限 200MB
- **手机扫码打开文件展示页**：
  - 显示文件名、大小、MIME 类型
  - 大「下载文件」按钮（强制 attachment 下载）
  - 安全提示：「仅下载来自可信来源的文件」

### 服务端改造

- **POST /api/upload** 新增 `kind=file` 参数：
  - 图片模式（默认）：5MB 上限，必须 image/*
  - 文件模式（`?kind=file`）：200MB 上限，接受任意 MIME
- **新增 GET /f/:code**：返回文件二进制
  - Content-Disposition: attachment（强制下载，浏览器不会尝试打开 APK/EXE）
  - 文件名按 RFC 5987 编码（支持中文）
  - 流式输出（io.Copy），大文件不爆内存
- **新增 viewFilePage**：文件信息 + 下载按钮的展示页
- **文件名清理 sanitizeFilename**：防路径穿越 + 控制字符
- 存储分层：元数据在 links.json；文件二进制在 data/files/<code>.<ext>
- 文件上传独立频率限制：每 IP 每分钟 3 次
- pruneExpired 和惰性删除统一调 removeLinkFile（按 Kind 删对应目录）

### 文件类型图标（emoji）

APK / 压缩包 / PDF / Word / Excel / PPT / 音频 / 视频 / Windows 可执行 / macOS / 文本 / 配置 / 其他

### 设计原则

- **扩展只做"上传入口 + 生成二维码"**，不参与下载（手机扫码后直接跟服务端通信）
- **不拦截浏览器下载**：用户下载行为不应被扩展接管
- **强制下载**：Content-Disposition: attachment，避免浏览器尝试打开可执行文件

---

## [1.2.4] - 2026-07-19

### 🎨 compact 模式：自动识别图片时二维码不滚动可见

之前自动上传后弹出预览 + 状态 + 二维码 + 短链可能需要滚动才能看到二维码。

**新增 compact 模式（自动识别到图片 URL 时启用）：**
- 预览图缩小到 56x56 浮动小角标（不占布局）
- 隐藏 label 和「生成二维码」按钮（自动模式不需要）
- 二维码从 240px 缩到 200px
- 总高度从 ~700px 降到 ~444px，远小于 Chrome popup 600px 上限
- 用户手动选文件时自动退出 compact 模式

---

## [1.2.3] - 2026-07-19

### ✨ 识别为图片后直接自动上传

之前识别为图片后只切到图片 Tab 让用户点按钮，多一步操作。
现在 HEAD 确认 Content-Type 是图片后，**直接抓图 + 上传 + 出二维码**，全程零点击。

popup 打开 → HEAD 验证 → 是图片 → 自动切图片 Tab → 自动上传 → 出二维码。

---

## [1.2.2] - 2026-07-19

### 🐛 修复：无后缀图片 URL 无法识别（如 ChatGPT/飞书/SaaS）

之前依赖 URL 后缀 + query 关键词识别，导致大量无后缀图片 URL（API、签名链接等）漏判。

**新策略：**
- 明显是图片后缀（.png/.jpg/.webp/.gif/...）→ 立即识别（不发请求）
- 明显是网页（.html/.php / 末尾斜杠等）→ 跳过（不发请求）
- **其他所有 URL**（无后缀的 API URL、签名链接等）→ 发 HEAD 请求验证 Content-Type

这样 chatgpt.com/backend-api/estuary/content?id=... 这种 URL 也能正确识别为图片，
因为服务端会返回 Content-Type: image/png。

利用扩展 SW fetch 绕过 CORS，对各种 SaaS 图床都有效。

---

## [1.2.1] - 2026-07-19

### ✨ 智能识别：地址栏是图片时自动切到图片 Tab

popup 打开时检测当前标签页 URL，识别是不是图片：

- **URL 后缀明显**（.png/.jpg/.jpeg/.webp/.gif/.bmp/.svg/.avif）→ 立即识别
- **URL 看起来像图片**（query 含 image/photo/pic/img 等）→ 发 HEAD 请求验证 Content-Type
- **识别为图片**：自动切到「📷 图片」Tab，显示预览 + 提示「点按钮上传」
- **识别为普通网页**：保持默认「🔗 链接」Tab（原逻辑）

技术上利用扩展 SW fetch 绕过 CORS 和多数防盗链，对图床直链特别有效。
不识别时也不影响其他 Tab 的正常使用。

---

## [1.2.0] - 2026-07-19

### ✨ 新增：图片上传 → 扫码看图

支持三种入口上传图片，生成短链二维码，手机扫码打开图片页（长按可保存）。

- **popup 新增「📷 图片」Tab**
  - 「📁 选择图片」按钮 + 拖拽上传
  - 在 Tab 内 Ctrl+V 粘贴截图（支持系统截屏）
  - 选完显示预览 + 大小提示，5MB 上限
- **「📝 文本」Tab 粘贴图片自动切换**
  - 在文本框粘贴图片时，自动切到图片 Tab 走上传流程
- **右键网页图片 → 「📷 此图传到服务器生成二维码」**
  - 利用 SW fetch 绕过 CORS 和多数防盗链（请求带着页面 Cookie/Referer）
  - 对服务器来说请求就像来自当前浏览器，大多数图床都能抓到

### 🖥️ 服务端：图片存储端点

- 新增 `POST /api/upload` — multipart 图片上传，返回短码（每 IP 每分钟 5 次频率限制）
- 新增 `GET /img/:code` — 返回原始图片二进制，带正确 Content-Type + 缓存头
- 新增 `GET /s/:code`（按类型分派）—— 图片类型返回带 `<img>` 的展示页 + 右上角「💾 保存」按钮
- 支持 PNG/JPEG/GIF/WebP/BMP（SVG 暂不支持，安全考虑）
- 服务端用 `http.DetectContentType` 嗅探真实 MIME（防伪造扩展名）
- 存储分层：元数据在 `links.json`，图片二进制在 `data/images/<code>.<ext>`
- 7 天过期，过期/惰性清理同步删文件
- 大小硬限 5MB（前端 + 后端双校验）

### 🔧 改进

- `config.js` 同时挂载到 `window` 和 `self`，支持 Service Worker 加载
- popup.js 在 shorten/upload 前重新读一次配置（用户在设置页改的值立即生效）
- background.js 新增 `loadConfig()`，SW 也能读 `chrome.storage.sync` 里的配置

---

## [1.1.1] - 2026-07-18

### ✨ 新增：设置页（Options Page）

- popup 右上角加 **⚙️ 齿轮按钮**，点击打开设置页
- 可配置「服务器地址」和「访问令牌」
- 配置存 `chrome.storage.sync`，**通过 Chrome 账号自动跨设备同步**
- **🧪 测试连接**按钮：一键验证服务端是否可达
- **↩️ 恢复默认**按钮：一键回到内置默认服务器
- 保留 `config.js` 作为开箱即用的默认值——不配置也能直接用

---

## [1.1.0] - 2026-07-18

### ✨ 新增：文本转短链二维码（搭配远程服务端）

- **弹窗新增 Tab 切换**：「🔗 链接」（原逻辑）和「📝 文本」（新功能）
- **文本 Tab**：粘贴任意长文本 → 远程服务转短链 → 生成短链二维码
  - 手机扫码后打开一个 HTML 页面，展示文本内容
  - 右上角「📋 复制」按钮，一键复制全文（手机端拇指易达）
  - 内容保留 7 天后自动清理
- **触发方式**：回车直接生成 / Shift+Enter 换行 / 点「🔲 生成二维码」按钮
- **失败降级**：服务端不可达时自动降级为本地编码原始文本（依然可用，只是二维码较密）

### 🖥️ 新增：Go 远程短链服务（`server/`）

- 单文件 Go HTTP 服务，零第三方依赖（纯标准库）
- 端点：
  - `POST /api/shorten` — 接收文本，返回 6 位 base62 短码（需 `X-Auth-Token` 鉴权）
  - `GET /s/:code` — 返回内联 HTML 展示页（含一键复制按钮）
  - `GET /api/health` — 健康检查
- JSON 文件存储，原子写入（tmp + rename）
- 每 IP 每分钟 10 次频率限制
- 过期清理：启动扫描 + 每小时循环 + 惰性删除
- 5.1MB 静态二进制，内存占用约 4MB

### 🏗️ 项目结构重构

- `extension/` — Chrome 扩展代码整体迁入（原有功能完全保留）
- `server/` — Go 远程短链服务
- `extension/config.js` — 集中管理远程服务 BASE_URL / AUTH_TOKEN
- 打包 workflow 改为只打包 `extension/`（服务端不进扩展包）

### 🔧 其他改进

- 服务端断开时扩展优雅降级，不报错卡死
- 一键部署脚本 `server/deploy.sh`（交叉编译 + 上传 + pm2 重启）

---

## [1.0.0] - 2025-06-27

### 🎉 首个正式版本

#### ✨ 新增功能

- **弹窗生成二维码**：点击工具栏图标，显示当前页面 URL 及对应二维码，支持输入自定义内容后回车刷新
- **右键解析图片二维码**：在 `<img>` 图片上右键，选择"解析图片中的二维码"，自动识别并显示内容
- **右键解析 Canvas 二维码**：在 Canvas 元素上右键，选择"解析 Canvas 中的二维码"，捕获画布内容进行解码
- **右键快捷生成**：
  - 链接上右键 → 生成链接的二维码
  - 选中文字右键 → 生成文字的二维码
  - 页面空白处右键 → 生成当前页面 URL 的二维码
- **结果浮层**：解析/生成结果以浮层展示，支持复制内容、打开链接、下载二维码图片
- **UTF-8 完整支持**：中文内容生成与解码无乱码

#### 🛠️ 技术实现

- Manifest V3 规范
- Service Worker 后台处理
- Content Script 捕获页面元素数据
- jsQR 纯 JS 解码
- OffscreenCanvas 无头渲染

#### 📦 打包发布

- GitHub Actions 自动打包为 `.zip` 发布包
- 提供 Release 下载
