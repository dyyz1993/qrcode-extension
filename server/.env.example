# 服务端配置示例 —— 复制为 .env 后按实际填写
# 端口（本地监听）
PORT=3031
# 鉴权 token，必须与 extension/config.js 中的 AUTH_TOKEN 一致
AUTH_TOKEN=请改成你自己的随机字符串
# 外部访问的根地址（用于拼接短链 URL）
BASE_URL=https://qrcode.shanbox.19930810.xyz:8443
# 数据目录
DATA_DIR=/root/qrcode-server/data
